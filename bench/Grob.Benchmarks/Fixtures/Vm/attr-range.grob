// Allocation-attribution fixture (phase 1, throwaway): range-loop machinery.
// 1,000-iteration numeric range for...in with an empty body — isolates loop
// setup/teardown and iteration bookkeeping from any native call cost.
for i in 1..1000 { }
