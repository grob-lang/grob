// E5902 — a top-level initialiser reads a top-level binding declared later
readonly tagline := buildTagline()

fn buildTagline(): string {
    return product
}

readonly product := "Grob"

print(tagline)
